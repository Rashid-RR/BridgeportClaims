import { Routes } from '@angular/router';
import { ReferralDefaultComponent } from './default/default.component';

export const ReferralRoutes: Routes = [
    {path: '', component: ReferralDefaultComponent}
];
