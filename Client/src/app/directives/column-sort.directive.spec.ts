import { ColumnSortDirective } from './column-sort.directive';

describe('ColumnSortDirective', () => {
  it('should create an instance', () => {
    const directive = new ColumnSortDirective(null);
    expect(directive).toBeTruthy();
  });
});
